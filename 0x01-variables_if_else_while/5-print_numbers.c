#include <stdio.h>
/**
 * main - This programm prints digits from 0 to 9
 * Return: returning 0
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}

	printf("\n");

	return (0);
}
