#include <unistd.h>
#include "holberton.h"
/**
 * mul - This programm checks for alphabetic character.
 * @a: The first number to mult
 * @b: The second number to mult
 *
 * Return: returning a mul
 */
int mul(int a, int b)
{
	return (a * b);
}
